export class Command {
    public loadConfigFile(configFilePath: string) {
    }
    public mergeCommandLineArgsIntoConfig(args: any, config: any) {
    }
    run(): any {
    }
    constructor(
        thekla: any,
        args: any
    ) {
    }
}